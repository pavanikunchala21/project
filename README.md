# Student Management System (SQL)

Simple Student Management System with a MySQL schema and sample queries.

## What’s included
- `schema.sql` — database schema (tables & constraints)
- `seed.sql` — sample data to populate DB
- `queries.sql` — CRUD examples and reports
- `ER-diagram.png` — optional ER diagram

## Setup (local MySQL)
1. Create DB and run schema:
   ```bash
   mysql -u root -p < schema.sql
